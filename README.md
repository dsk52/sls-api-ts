# sls-api-ts

```
$ npm i
```

```
// check
$ sls invoke local --function api

// local dev
$ sls offline
:
offline: Offline [http for lambda] listening on http://localhost:3000

// deploy
$ sls deploy
```
